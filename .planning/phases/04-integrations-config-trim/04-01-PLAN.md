---
phase: 04-integrations-config-trim
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VeriHash.Config.ps1
  - Tests/VeriHash.Config.Tests.ps1
  - VeriHash.ps1
  - VeriHash.LogUtils.ps1
  - Tests/VeriHash.Tests.ps1
  - Tests/VeriHash.HotPath.Tests.ps1
  - Tests/VeriHash.Manifest.Module.Tests.ps1
autonomous: true
requirements: [CFG-01, CFG-02, CFG-03]

must_haves:
  truths:
    - "Select-String -i 'virustotal|VERIHASH_VT_' over *.ps1, *.psm1, *.psd1 recursively returns zero matches"
    - "Select-String -i 'virustotal|VERIHASH_VT_' over Tests/ returns zero matches"
    - "Select-String -i 'PSFramework|Write-PSFMessage|PSFrameworkAvailable' over entire source + tests tree returns zero matches"
    - "Full test suite (Test-All.ps1) passes without PSFramework installed"
  artifacts:
    - path: "VeriHash.Config.ps1"
      provides: "Config service with no VT fields, no PSFramework guards"
      contains: "logging = @{"
    - path: "Tests/VeriHash.Config.Tests.ps1"
      provides: "Config tests with no VT test cases"
    - path: "VeriHash.ps1"
      provides: "Main script with no PSFramework init region, no guard sites"
    - path: "VeriHash.LogUtils.ps1"
      provides: "LogUtils with PSFramework comment mentions removed"
    - path: "Tests/VeriHash.Tests.ps1"
      provides: "Smoke tests with PSFramework comment mentions removed"
    - path: "Tests/VeriHash.HotPath.Tests.ps1"
      provides: "HotPath tests with PSFramework guard test removed"
    - path: "Tests/VeriHash.Manifest.Module.Tests.ps1"
      provides: "Manifest tests with PSFramework guard test removed"
  key_links:
    - from: "VeriHash.Config.ps1"
      to: "Tests/VeriHash.Config.Tests.ps1"
      via: "Config function behavior"
      pattern: "Get-VeriHashDefaultConfig|Set-VeriHashConfig"
---

<objective>
Remove all VirusTotal config scaffolding and all PSFramework references from the entire codebase — production source and tests.

Purpose: CFG-01/CFG-02/CFG-03 require zero matches for VirusTotal and PSFramework patterns across the source tree. This plan performs all deletion/comment-editing needed to satisfy the three success criteria greps. Once this lands, Plan 02 can extract integration functions from already-clean code.

Output: 7 modified files, all VT/PSFramework dead code eliminated, test suite green.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-integrations-config-trim/04-CONTEXT.md
@.planning/phases/04-integrations-config-trim/04-RESEARCH.md
@.planning/phases/04-integrations-config-trim/04-PATTERNS.md

<interfaces>
<!-- Key context the executor needs — extracted from research artifacts. -->

## VirusTotal Removal — Exact Lines in VeriHash.Config.ps1

Lines to DELETE:
- 85-91: `virustotal = @{ apiKey = ''; enabled = $false; preferApi = $true; autoOpen = $false }` in Get-VeriHashDefaultConfig
- 109-110: Docstring lines mentioning VERIHASH_VT_APIKEY and VERIHASH_VT_ENABLED
- 151-154: `$source.'virustotal.*' = 'default'` initializers (4 lines)
- 191-209: `# Merge virustotal settings from file` block (entire if block)
- 254-261: `if ($env:VERIHASH_VT_APIKEY)` and `if ($env:VERIHASH_VT_ENABLED)` blocks
- 271-272: VTEnabled/VTHasApiKey in PSF guard Data (already inside a PSF block — covered by PSF removal)
- 337: `virustotal = $Config.virustotal` in Set-VeriHashConfig `$configToSave`

After removal: Get-VeriHashDefaultConfig returns only `@{ logging = @{ ... } }`. Set-VeriHashConfig's $configToSave contains only `logging`.

## VirusTotal Removal — Exact Lines in Tests/VeriHash.Config.Tests.ps1

Lines to DELETE:
- 20-21: `$env:VERIHASH_VT_APIKEY = $null; $env:VERIHASH_VT_ENABLED = $null` in AfterAll
- 76-82: Entire `It 'Returns a hashtable with virustotal section'` test
- 94-103: Entire `It 'Has correct default virustotal values'` test
- 113-114: `$env:VERIHASH_VT_APIKEY/ENABLED = $null` in BeforeEach
- 127: `$result.virustotal.enabled | Should -Be $false` assertion
- 143-148: `virustotal = @{ ... }` in test config file data
- 157-158: `$result.virustotal.*` assertions
- 180: `$result.virustotal.enabled | Should -Be $false`
- 202-216: Entire `It 'Environment variable overrides config file for VirusTotal API key'` test
- 219-231: VT assertions inside boolean env var test
- 244, 252: VT source tracking assertions
- 264-265: `$env:VERIHASH_VT_APIKEY/ENABLED = $null` in Set-VeriHashConfig BeforeEach
- 279-284: `virustotal` section in test config
- 301: `virustotal = @{ apiKey = 'test' }` line
- 338-343: `virustotal` section in round-trip test
- 353-354: `$loaded.virustotal.*` round-trip assertions

## PSFramework Removal — VeriHash.Config.ps1 (14 guard sites)

All blocks matching this pattern:
```powershell
if ($script:PSFrameworkAvailable) {
    Write-PSFMessage -Level <Level> -Message "<msg>" -Tag '<tags>' -Data @{
        <key> = <value> | ConvertTo-SanitizedPath
    }
}
```
Lines: 157-162, 176-178, 211-215, 219-221, 224-228, 238-240, 265-273, 317-321, 327-330, 344-347, 390-394, 400-403, 409-411, 419-422.

Delete silently — no replacement (D-04).

## PSFramework Removal — VeriHash.ps1 (20 guard sites + init region)

- Line 98-99: Comment + `$script:PSFrameworkAvailable = ...` declaration
- Lines 101-105: `#region Module Imports` — remove VeriHash.LogUtils.ps1 dot-source (line 103), keep VeriHash.Config.ps1 dot-source (line 104). Per Pitfall 6: all LogUtils consumers were inside PSF blocks.
- Lines 107-187: Entire `#region PSFramework Logging Initialization` — delete entire region (D-04, config loading + LogLevel logic feeds INTO PSF init, becomes dead code)
- Lines 294-296, 328-333: PSF guards in Install-WindowsSendTo
- Lines 353-357, 401-405, 531-537: PSF guards in Install-LinuxContextMenu / Install-KDEContextMenu
- Lines 668-671, 744-745, 752-753, 760-761, 768-769: PSF guards in Get-ClipboardHash
- Lines 788-797, 811-821: PSF guards in Get-And-SaveHash
- Lines 1035-1044: PSF guard in Invoke-HashFile
- Lines 1401-1405, 1408-1410, 1419-1421, 1432-1437, 1504-1514: PSF guards in Test-HashSidecar
- Line 1400: Standalone `$sanitizedSidecarPath = $SidecarPath | ConvertTo-SanitizedPath` — delete (dead variable after guard removal)

## PSFramework Removal — VeriHash.LogUtils.ps1 (3 comments only)

- Line 14: `- PSFramework adds ComputerName/Username metadata (cannot be disabled)` → reword to remove PSFramework mention
- Line 218: `# Skip lines that are just commas (PSFramework JSON array format artifact)` → reword
- Line 220: `# Strip trailing comma from JSON lines (PSFramework appends commas for array format)` → reword

## PSFramework Removal — Test files

- Tests/VeriHash.HotPath.Tests.ps1 lines 39-42: Delete entire `It 'No PSFramework references in VeriHash.HotPath/'` block (D-05)
- Tests/VeriHash.Manifest.Module.Tests.ps1 lines 37-40: Delete entire `It 'No PSFramework references in VeriHash.Manifest/'` block (D-05)
- Tests/VeriHash.Tests.ps1 line 14: Reword comment to remove "PSFramework Logging" (D-05)
- Tests/VeriHash.Tests.ps1 line 22: Reword comment to remove PSFramework mention (D-05)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove VirusTotal scaffolding from VeriHash.Config.ps1 and Tests/VeriHash.Config.Tests.ps1</name>
  <files>VeriHash.Config.ps1, Tests/VeriHash.Config.Tests.ps1</files>
  <read_first>
    - VeriHash.Config.ps1 (full file — identify all virustotal/VERIHASH_VT_ lines)
    - Tests/VeriHash.Config.Tests.ps1 (full file — identify all virustotal/VERIHASH_VT_ lines and test blocks)
    - .planning/phases/04-integrations-config-trim/04-RESEARCH.md (lines 246-285 — exact line inventory)
    - .planning/phases/04-integrations-config-trim/04-PATTERNS.md (lines 277-408 — exact code excerpts to delete)
  </read_first>
  <action>
**VeriHash.Config.ps1** — remove ALL VirusTotal references per CFG-01 and D-07/D-08:

1. In `Get-VeriHashDefaultConfig`: delete the `virustotal = @{ ... }` block (lines ~85-91). The function should return only:
   ```powershell
   @{
       logging = @{
           level   = 'INFO'
           file    = $true
           console = $true
       }
   }
   ```

2. In `Get-VeriHashConfig` docstring: delete the two lines mentioning `VERIHASH_VT_APIKEY` and `VERIHASH_VT_ENABLED` (lines ~109-110).

3. In `Get-VeriHashConfig` body: delete the 4 source-tracking initializer lines `$source.'virustotal.*' = 'default'` (lines ~151-154).

4. In `Get-VeriHashConfig` body: delete the entire `# Merge virustotal settings from file` block including the outer `if ($fileContent.virustotal) { ... }` (lines ~191-209).

5. In `Get-VeriHashConfig` body: delete the `if ($env:VERIHASH_VT_APIKEY)` block and the `if ($env:VERIHASH_VT_ENABLED)` block (lines ~254-261).

6. In `Set-VeriHashConfig`: remove `virustotal = $Config.virustotal` from the `$configToSave` hashtable (line ~337). Result should be:
   ```powershell
   $configToSave = @{
       logging = $Config.logging
   }
   ```

**Tests/VeriHash.Config.Tests.ps1** — remove ALL VirusTotal references per CFG-02:

1. In `AfterAll`: delete `$env:VERIHASH_VT_APIKEY = $null` and `$env:VERIHASH_VT_ENABLED = $null` (lines ~20-21).

2. Delete entire test: `It 'Returns a hashtable with virustotal section'` (lines ~76-82).

3. Delete entire test: `It 'Has correct default virustotal values'` (lines ~94-103).

4. In `Get-VeriHashConfig` `BeforeEach`: delete `$env:VERIHASH_VT_APIKEY = $null` and `$env:VERIHASH_VT_ENABLED = $null` (lines ~113-114).

5. Remove assertion `$result.virustotal.enabled | Should -Be $false` from the defaults test (line ~127).

6. Remove `virustotal = @{ apiKey = 'test-api-key'; enabled = $false; preferApi = $false; autoOpen = $true }` from test config data (lines ~143-148).

7. Remove `$result.virustotal.*` assertions (lines ~157-158).

8. Remove `$result.virustotal.enabled | Should -Be $false` (line ~180).

9. Delete entire test: `It 'Environment variable overrides config file for VirusTotal API key'` (lines ~202-216).

10. Edit the boolean env var test (lines ~219-231) to remove VT-specific assertions while keeping the logging assertions intact.

11. Remove VT source tracking assertions at lines ~244 and ~252.

12. In `Set-VeriHashConfig` `BeforeEach`: delete `$env:VERIHASH_VT_APIKEY = $null` and `$env:VERIHASH_VT_ENABLED = $null` (lines ~264-265).

13. Remove `virustotal = @{ ... }` sections from test config data in Set-VeriHashConfig tests (lines ~279-284, ~301, ~338-343).

14. Remove `$loaded.virustotal.*` round-trip assertions (lines ~353-354).

**Verification approach:** After all edits, run `Select-String` to confirm zero VT matches in both files, then run Config tests to ensure they pass.
  </action>
  <verify>
    <automated>
cd C:\Users\bean\BitBox\source\repos\VeriHash
$vtHits = Select-String -Path VeriHash.Config.ps1,Tests/VeriHash.Config.Tests.ps1 -Pattern 'virustotal|VERIHASH_VT_' -CaseSensitive:$false
if ($vtHits) { $vtHits | ForEach-Object { Write-Error $_.ToString() }; throw "VirusTotal references remain" }
Write-Host "PASS: Zero VirusTotal references"
Invoke-Pester -Path Tests/VeriHash.Config.Tests.ps1 -Output Detailed
    </automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path VeriHash.Config.ps1 -Pattern 'virustotal|VERIHASH_VT_'` returns zero matches
    - `Select-String -Path Tests/VeriHash.Config.Tests.ps1 -Pattern 'virustotal|VERIHASH_VT_'` returns zero matches
    - `Get-VeriHashDefaultConfig` returns a hashtable with only a `logging` key (no `virustotal` key)
    - `VeriHash.Config.ps1` contains `$configToSave = @{` followed by only `logging = $Config.logging`
    - `Invoke-Pester -Path Tests/VeriHash.Config.Tests.ps1` exits with zero failures
  </acceptance_criteria>
  <done>All VirusTotal fields, defaults, env-var handling, and test cases removed. Config tests pass. Zero VT pattern matches in Config source + tests.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all PSFramework references from production source and test files</name>
  <files>VeriHash.ps1, VeriHash.Config.ps1, VeriHash.LogUtils.ps1, Tests/VeriHash.Tests.ps1, Tests/VeriHash.HotPath.Tests.ps1, Tests/VeriHash.Manifest.Module.Tests.ps1</files>
  <read_first>
    - VeriHash.ps1 (full file — identify all PSFrameworkAvailable guards, the init region, the LogUtils dot-source)
    - VeriHash.Config.ps1 (full file — after Task 1 edits — identify remaining PSFrameworkAvailable guards)
    - VeriHash.LogUtils.ps1 (full file — identify 3 PSFramework comment mentions at lines 14, 218, 220)
    - Tests/VeriHash.Tests.ps1 (lines 1-30 — identify PSFramework comment mentions at lines 14, 22)
    - Tests/VeriHash.HotPath.Tests.ps1 (lines 35-45 — identify PSFramework guard test at lines 39-42)
    - Tests/VeriHash.Manifest.Module.Tests.ps1 (lines 33-45 — identify PSFramework guard test at lines 37-40)
    - .planning/phases/04-integrations-config-trim/04-RESEARCH.md (lines 155-243 — complete PSFramework inventory)
    - .planning/phases/04-integrations-config-trim/04-PATTERNS.md (lines 342-588 — PSFramework guard deletion pattern)
  </read_first>
  <action>
**VeriHash.Config.ps1** — delete 14 PSFramework guard blocks per D-04:

Delete ALL `if ($script:PSFrameworkAvailable) { ... }` blocks at the line ranges documented in the research: 157-162, 176-178, 211-215, 219-221, 224-228, 238-240, 265-273, 317-321, 327-330, 344-347, 390-394, 400-403, 409-411, 419-422. Each block is 2-7 lines. Delete the entire `if` statement including its body. Do NOT replace with `Write-Verbose`, `Write-VeriHashLog`, or anything else (per D-04: "Delete silently — no replacement").

**VeriHash.ps1** — delete 20 guard sites + PSF init region + cleanup:

1. Delete `$script:PSFrameworkAvailable` comment and variable declaration (lines ~98-99):
   ```powershell
   # Check if PSFramework is available (single authoritative check — used by all modules)
   $script:PSFrameworkAvailable = $null -ne (Get-Module -ListAvailable -Name PSFramework)
   ```

2. In `#region Module Imports` (lines ~101-105): Remove the `VeriHash.LogUtils.ps1` dot-source line (line ~103: `. "$PSScriptRoot\VeriHash.LogUtils.ps1"`). Keep the `VeriHash.Config.ps1` dot-source (line ~104: `. "$PSScriptRoot\VeriHash.Config.ps1"`). Update or remove the comment about "Config depends on ConvertTo-SanitizedPath" since that dependency is now dead.

3. Delete the entire `#region PSFramework Logging Initialization` through its `#endregion` (lines ~107-187). This includes `$script:VeriHashConfig = Get-VeriHashConfig`, LogLevel priority logic, and the `if ($script:PSFrameworkAvailable) { ... } else { ... }` init block. All of it fed into PSFramework; it's all dead code now (D-04).

4. Delete PSFramework guards inside install functions (these functions still live in VeriHash.ps1 until Plan 02 extracts them):
   - Lines ~294-296 in Install-WindowsSendTo (log install attempt)
   - Lines ~328-333 in Install-WindowsSendTo (log success)
   - Lines ~353-357 in Install-LinuxContextMenu (log install attempt)
   - Lines ~401-405 in Install-KDEContextMenu (log install attempt)
   - Lines ~531-537 in Install-KDEContextMenu (log success)

5. Delete PSFramework guards in Get-ClipboardHash:
   - Lines ~668-671 (log clipboard check entry)
   - Lines ~744-745 (log MD5 detected)
   - Lines ~752-753 (log SHA256 detected)
   - Lines ~760-761 (log SHA512 detected)
   - Lines ~768-769 (log no hash found)

6. Delete PSFramework guards in Get-And-SaveHash:
   - Lines ~788-797 (log hash computation start)
   - Lines ~811-821 (log hash result)

7. Delete PSFramework guard in Invoke-HashFile:
   - Lines ~1035-1044 (log function entry)

8. Delete PSFramework guards in Test-HashSidecar + orphaned variable:
   - Line ~1400: Delete standalone `$sanitizedSidecarPath = $SidecarPath | ConvertTo-SanitizedPath` (dead variable — only used inside PSF blocks below, per Pitfall 2)
   - Lines ~1401-1405 (log sidecar verify entry)
   - Lines ~1408-1410 (log sidecar not found)
   - Lines ~1419-1421 (log empty sidecar)
   - Lines ~1432-1437 (log processing entries)
   - Lines ~1504-1514 (log verify summary)

**VeriHash.LogUtils.ps1** — edit 3 comment lines (per D-05, to satisfy Select-String zero-match):

1. Line 14: Change `- PSFramework adds ComputerName/Username metadata (cannot be disabled)` to `- Legacy structured logging added ComputerName/Username metadata (retired in v2)` or similar wording that removes the word "PSFramework".

2. Line 218: Change `# Skip lines that are just commas (PSFramework JSON array format artifact)` to `# Skip lines that are just commas (legacy JSON array format artifact)`.

3. Line 220: Change `# Strip trailing comma from JSON lines (PSFramework appends commas for array format)` to `# Strip trailing comma from JSON lines (legacy logger appends commas for array format)`.

**Tests/VeriHash.Tests.ps1** — edit 2 comment lines (per D-05):

1. Line 14: Change `PSFramework Logging` to `Logging` (or remove the mention entirely).

2. Line 22: Change `PSFramework Logging    -> retired in Phase 1; replaced by Write-VeriHashLog` to `Logging -> Write-VeriHashLog (Core, Phase 1)` or similar wording that removes "PSFramework".

**Tests/VeriHash.HotPath.Tests.ps1** — delete guard test (per D-05):

Delete the entire `It 'No PSFramework references in VeriHash.HotPath/'` block (lines ~39-42):
```powershell
    It 'No PSFramework references in VeriHash.HotPath/' {
        $hits = Get-ChildItem "$PSScriptRoot/../VeriHash.HotPath" -Recurse -File |
            Select-String -Pattern 'PSFramework|Write-PSFMessage' -ErrorAction SilentlyContinue
        $hits | Should -BeNullOrEmpty
    }
```

**Tests/VeriHash.Manifest.Module.Tests.ps1** — delete guard test (per D-05):

Delete the entire `It 'No PSFramework references in VeriHash.Manifest/'` block (lines ~37-40):
```powershell
    It 'No PSFramework references in VeriHash.Manifest/' {
        $hits = Get-ChildItem "$PSScriptRoot/../VeriHash.Manifest" -Recurse -File |
            Select-String -Pattern 'PSFramework|Write-PSFMessage' -ErrorAction SilentlyContinue
        $hits | Should -BeNullOrEmpty
    }
```

**ConvertTo-SanitizedPath in VeriHash.LogUtils.ps1:** Leave as-is per D-06. The function definition stays as dead code. Phase 5 decides its fate.

**Verification approach:** After all edits, run the three success criteria greps over the entire source + test tree, then run the full test suite.
  </action>
  <verify>
    <automated>
cd C:\Users\bean\BitBox\source\repos\VeriHash
$psfHits = Get-ChildItem -Path . -Recurse -Include '*.ps1','*.psm1','*.psd1' | Select-String -Pattern 'PSFramework|Write-PSFMessage|PSFrameworkAvailable' -CaseSensitive:$false
if ($psfHits) { $psfHits | ForEach-Object { Write-Error $_.ToString() }; throw "PSFramework references remain" }
Write-Host "PASS: Zero PSFramework references"
.\Test-All.ps1 -SkipProfiler
    </automated>
  </verify>
  <acceptance_criteria>
    - `Get-ChildItem -Recurse -Include '*.ps1','*.psm1','*.psd1' | Select-String -Pattern 'PSFramework|Write-PSFMessage|PSFrameworkAvailable'` returns zero matches
    - VeriHash.ps1 does NOT contain the string `$script:PSFrameworkAvailable`
    - VeriHash.ps1 does NOT contain the string `VeriHash.LogUtils.ps1` (dot-source removed)
    - VeriHash.ps1 does NOT contain `#region PSFramework`
    - VeriHash.Config.ps1 does NOT contain `Write-PSFMessage`
    - VeriHash.LogUtils.ps1 does NOT contain the literal string `PSFramework` (comments reworded)
    - Tests/VeriHash.HotPath.Tests.ps1 does NOT contain `PSFramework`
    - Tests/VeriHash.Manifest.Module.Tests.ps1 does NOT contain `PSFramework`
    - Tests/VeriHash.Tests.ps1 does NOT contain `PSFramework`
    - `.\Test-All.ps1 -SkipProfiler` exits with zero test failures
    - ConvertTo-SanitizedPath function definition still exists in VeriHash.LogUtils.ps1 (per D-06)
  </acceptance_criteria>
  <done>All 34 PSFramework guard blocks deleted from production source. PSF init region deleted from VeriHash.ps1. LogUtils dot-source removed from VeriHash.ps1. 3 comments in LogUtils reworded. 2 comments in VeriHash.Tests.ps1 reworded. 2 guard tests deleted from HotPath/Manifest tests. Full test suite green with zero PSFramework pattern matches anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file → Config loader | Untrusted JSON from user's config directory |
| Env vars → Config loader | Untrusted environment variable values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T (Tampering) | VeriHash.Config.ps1 | accept | Config loader already validates known keys only — removing VT fields reduces attack surface. Existing validation remains. |
| T-04-02 | I (Information Disclosure) | PSFramework removal | mitigate | PSFramework logged potentially sensitive data (ComputerName, paths). Removal eliminates this channel entirely. |
</threat_model>

<verification>
After both tasks complete:
1. `Select-String -i 'virustotal|VERIHASH_VT_' over all *.ps1, *.psm1, *.psd1 recursively` → zero matches
2. `Select-String -i 'PSFramework|Write-PSFMessage|PSFrameworkAvailable' over all *.ps1, *.psm1, *.psd1 recursively` → zero matches
3. `.\Test-All.ps1 -SkipProfiler` → all tests pass, zero failures
4. `ConvertTo-SanitizedPath` still defined in VeriHash.LogUtils.ps1 (D-06 preserved)
</verification>

<success_criteria>
- CFG-01 satisfied: zero VirusTotal matches in production source
- CFG-02 satisfied: zero VirusTotal matches in test files
- CFG-03 satisfied: zero PSFramework matches in entire source + test tree
- Full test suite passes without PSFramework installed
- ConvertTo-SanitizedPath left as dead code per D-06
</success_criteria>

<output>
After completion, create `.planning/phases/04-integrations-config-trim/04-01-SUMMARY.md`
</output>
