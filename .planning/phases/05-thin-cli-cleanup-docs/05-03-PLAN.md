---
phase: 05-thin-cli-cleanup-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [CLEAN-03, CLEAN-04]

must_haves:
  truths:
    - "README documents v2 module architecture (Core + HotPath + Manifest + thin CLI)"
    - "README documents the new CLI surface with breaking changes called out"
    - "README documents simplified install path (no PSFramework)"
    - "README has v2.0 version badge"
    - "CHANGELOG has v2.0 entry listing removals, additions, and breaking changes"
    - "CHANGELOG v1 entries are collapsed under a details/summary section"
  artifacts:
    - path: "README.md"
      provides: "v2 documentation"
      contains: "## Module Architecture"
    - path: "CHANGELOG.md"
      provides: "v2.0 changelog entry"
      contains: "v2.0.0"
  key_links:
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "See full changelog link"
      pattern: "CHANGELOG\\.md"
---

<objective>
Rewrite README.md for the v2 architecture and add a v2.0.0 CHANGELOG entry with breaking changes.

Purpose: README must document the modular rewrite (Core + HotPath + Manifest + thin CLI), the new CLI surface with breaking changes called out, and the simplified install path. CHANGELOG must have a clean v2.0 entry framed as "Modular Rewrite" with removals/additions/breaking changes, and v1 history collapsed.

Output: Rewritten `README.md`, updated `CHANGELOG.md`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-thin-cli-cleanup-docs/05-CONTEXT.md
@.planning/phases/05-thin-cli-cleanup-docs/05-PATTERNS.md

<interfaces>
<!-- README references these. Extracted from project. -->

CLI surface (v2):
  .\VeriHash.ps1 <file>                  # SHA256 hash + sidecar + clipboard
  .\VeriHash.ps1 <files...>               # Multi-file batch with tally
  .\VeriHash.ps1 <files...> -Manifest     # Create sha256sum manifest
  .\VeriHash.ps1 <file.sha256> -Manifest  # Verify sha256sum manifest
  .\VeriHash.ps1 -InstallSendTo           # Install Windows/Linux context menu
  .\VeriHash.ps1 -InstallKDE              # Install KDE Dolphin context menu
  Switches: -Manifest, -InstallSendTo, -InstallKDE, -NoPause, -SystemWide, -Log, -Help

Dropped v1 params: -Hash, -Algorithm, -OnlyVerify, -SkipSignatureCheck, -Force, -LogLevel, -SendTo

Module structure:
  VeriHash.Core/    → Get-VeriHashResult, Format-VeriHashReport, Read-ClipboardHash,
                      Test-VeriHashSidecar, Write-VeriHashLog, Get-VeriHashPlatform
  VeriHash.HotPath/ → Invoke-VeriHashHotPath, Invoke-VeriHashBatch, Get-VeriHashSignature
  VeriHash.Manifest/ → New-VeriHashManifest, Test-VeriHashManifest
  VeriHash.Integrations.ps1 → Install-WindowsSendTo, Install-LinuxContextMenu

Badges (existing patterns):
  [![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)](...)
  [![PowerShell](https://img.shields.io/badge/PowerShell-7%2B-blue.svg)](...)
  [![License](https://img.shields.io/badge/license-AGPL--3.0-blue.svg)](LICENSE.md)
  [![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)](...)

Logo: ![VeriHash Logo](https://github.com/arcticpinecone/pwsh7-VeriHash/blob/main/Icons/VeriHash_256.webp?raw=true)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for v2 architecture</name>
  <files>README.md</files>
  <read_first>
    - README.md (current file — lines 1-60 for logo, badges, header structure; lines 31-48 for table of contents pattern; lines 51-65 for features section style)
    - .planning/phases/05-thin-cli-cleanup-docs/05-CONTEXT.md (D-09 section structure, D-12 version badge)
    - .planning/phases/05-thin-cli-cleanup-docs/05-PATTERNS.md (badge pattern, logo pattern, section heading pattern, color convention)
    - VeriHash.ps1 (the v2 thin CLI — to accurately document the param surface and dispatch behavior)
    - Microsoft.PowerShell_profile_example.ps1 (for profile integration section — if it exists)
  </read_first>
  <action>
Replace the entire contents of README.md with the v2 documentation. The structure MUST follow D-09 exactly, with D-12 version badge. Here is the complete structure:

**Header section:**
```markdown
# VeriHash

![VeriHash Logo](https://github.com/arcticpinecone/pwsh7-VeriHash/blob/main/Icons/VeriHash_256.webp?raw=true)

## Modular file integrity verification for PowerShell 7+

[![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)](https://github.com/arcticpinecone/pwsh7-VeriHash/releases)
[![PowerShell](https://img.shields.io/badge/PowerShell-7%2B-blue.svg)](https://github.com/PowerShell/PowerShell)
[![License](https://img.shields.io/badge/license-AGPL--3.0-blue.svg)](LICENSE.md)
[![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)](https://github.com/arcticpinecone/pwsh7-VeriHash)

> **v2.0 — Modular Rewrite** · [See what changed](CHANGELOG.md)

---
```
Note: version badge says 2.0.0 (per D-12). No inline release notes — link to CHANGELOG per D-12.

**Table of Contents (per D-09 section order):**
```markdown
## 📋 Table of Contents

- [Features](#-features)
- [Requirements](#-requirements)
- [Installation](#-installation)
- [Usage](#-usage)
- [OS Integration](#-os-integration)
- [Module Architecture](#-module-architecture)
- [Speed Benchmarks](#-speed-benchmarks)
- [Why PowerShell 7?](#-why-powershell-7)
- [PowerShell Profile Integration](#-powershell-profile-integration)
- [Privacy](#-privacy)
- [Contributing](#-contributing)
- [Running Tests](#-running-tests)
- [License](#-license)
```

**Section 1: Features**
```markdown
## ✨ Features

- 🔐 **Multiple Hash Algorithms**: SHA256 (default), MD5, SHA512
- ⚡ **Parallel Processing**: Hash + Authenticode signature run concurrently for PE files
- 🎯 **Smart Verification**: Auto-detects hash from clipboard (plain hex or `algo:hex` prefix)
- 📁 **Sidecar Files**: GNU-compatible `.sha256`, `.sha512`, `.md5` — create, verify, and resolve mismatches
- 📦 **Manifest Mode**: Create and verify `sha256sum`-compatible manifests with atomic writes
- 🔄 **Multi-File Batch**: Process multiple files in one invocation with match/mismatch/missing tally
- 🖥️ **Cross-Platform**: Windows, macOS, Linux
- ✅ **Digital Signatures**: PE-only Authenticode verification (skips non-PE files)
- 📋 **Clipboard Matching**: Automatic hash comparison from clipboard on all platforms
- 🐧 **Linux Integration**: KDE Dolphin context menu support
- 📝 **Plain-Text Logging**: One line per file to `~/.verihash/verihash.log` (opt-in)
```

**Section 2: Requirements**
```markdown
## 📋 Requirements

- **PowerShell 7.0+** (not Windows PowerShell 5.1)
  - Windows: `winget install Microsoft.PowerShell`
  - Linux: [Install instructions](https://learn.microsoft.com/en-us/powershell/scripting/install/installing-powershell-on-linux)
  - macOS: `brew install powershell`

No external dependencies. PSFramework is **not** required (removed in v2).
```

**Section 3: Installation**
```markdown
## 🚀 Installation

```powershell
# Clone the repository
git clone https://github.com/arcticpinecone/pwsh7-VeriHash.git
cd pwsh7-VeriHash

# Verify it works
.\VeriHash.ps1 -Help
```

No module installation or `Install-Module` needed — run directly from the cloned directory.
```

**Section 4: Usage** — Document all three modes: hash, verify, manifest
```markdown
## 💻 Usage

### Hash a single file
```powershell
.\VeriHash.ps1 file.exe
```
Computes SHA256, checks clipboard for a matching hash, checks for a sibling `.sha256` sidecar file, and displays Authenticode signature status for PE files.

### Hash multiple files
```powershell
.\VeriHash.ps1 file1.exe, file2.dll, file3.zip
```
Processes each file with full hash/sidecar/clipboard/signature checks, then prints a tally: `2/3 matched, 1 mismatch, 0 missing`.

### Create a manifest
```powershell
.\VeriHash.ps1 file1.txt, file2.txt -Manifest
```
Creates a `sha256sum`-compatible manifest in the files' parent directory. Atomic write (no partial manifests on Ctrl+C).

### Verify a manifest
```powershell
.\VeriHash.ps1 manifest.sha256 -Manifest
```
Extension auto-detect: `.sha256`, `.sha512`, `.md5` files are verified. Returns exit code 0 (all pass), 1 (mismatch), 2 (missing), 3 (parse error).

### Clipboard matching
Copy a hash to your clipboard before running VeriHash. Supported formats:
- Plain hex: `71792c028e07b0fdd30f...` (length auto-detects MD5/SHA256/SHA512)
- Prefixed: `sha256:71792c028e07b0fdd30f...` (prefix overrides length detection)

### Logging
```powershell
.\VeriHash.ps1 file.exe -Log
# or set environment variable:
$env:VERIHASH_LOG = '1'
```
Appends one line per file to `~/.verihash/verihash.log`.

### All switches
| Switch | Description |
|--------|-------------|
| `-Manifest` | Create or verify a sha256sum manifest |
| `-InstallSendTo` | Install Windows SendTo / Linux context menu |
| `-InstallKDE` | Install KDE Dolphin context menu |
| `-NoPause` | Never pause at exit |
| `-SystemWide` | System-wide install (Linux, requires sudo) |
| `-Log` | Log each file to `~/.verihash/verihash.log` |
| `-Help` | Show help |
```

**Section 5: OS Integration**
```markdown
## 🖥️ OS Integration

### Windows (SendTo)
```powershell
.\VeriHash.ps1 -InstallSendTo
```
Installs two shortcuts in your SendTo folder:
- **VeriHash** — right-click → Send to → VeriHash to hash files
- **VeriHash - Manifest** — right-click → Send to → create/verify manifest

### Linux (KDE Dolphin)
```powershell
# User-level
./VeriHash.ps1 -InstallSendTo

# System-wide (requires sudo)
sudo pwsh -File VeriHash.ps1 -InstallSendTo -SystemWide

# KDE-specific
./VeriHash.ps1 -InstallKDE
```
Creates `.desktop` service menu entries with Compute Hash, Verify Hash, and Manifest Hash actions.
```

**Section 6: Module Architecture** — New section per D-09
```markdown
## 🏗️ Module Architecture

VeriHash v2 is a modular rewrite. The ~900-line v1 monolith has been replaced with focused modules and a thin CLI dispatcher.

```
VeriHash.ps1                  ← Thin CLI dispatcher (≤200 lines)
│
├── VeriHash.Core/            ← Core module
│   ├── Get-VeriHashResult      Hash computation (MD5, SHA256, SHA512)
│   ├── Read-ClipboardHash      Clipboard auto-detect (plain hex + algo:hex)
│   ├── Test-VeriHashSidecar    Sidecar file verification
│   ├── Format-VeriHashReport   Formatted console output
│   ├── Write-VeriHashLog       Plain-text logging
│   └── Get-VeriHashPlatform    Cross-platform detection
│
├── VeriHash.HotPath/         ← Performance module
│   ├── Invoke-VeriHashHotPath  Single-file: parallel hash + Authenticode sig
│   ├── Invoke-VeriHashBatch    Multi-file loop with tally
│   └── Get-VeriHashSignature   PE-only Authenticode wrapper
│
├── VeriHash.Manifest/        ← Manifest module
│   ├── New-VeriHashManifest    Create sha256sum-compatible manifests
│   └── Test-VeriHashManifest   Verify manifests (exit codes 0/1/2/3)
│
└── VeriHash.Integrations.ps1 ← OS integration (lazy-loaded)
    ├── Install-WindowsSendTo   Windows SendTo shortcuts
    └── Install-LinuxContextMenu  KDE Dolphin .desktop entries
```

**Design principles:**
- **Modules own logic** — no business logic in the CLI script
- **CLI owns UX** — pause-at-end, help banner, manifest output rendering
- **Lazy loading** — Integrations.ps1 only loaded when `-InstallSendTo` or `-InstallKDE` is used
- **Testable** — modules loaded via `Import-Module`, conditional import in CLI enables mocking
```

**Section 7: Speed Benchmarks** — Adapt from existing README, remove v1 specifics
```markdown
## ⚡ Speed Benchmarks

VeriHash uses .NET's optimized hash streams and parallel ThreadJob execution:

| File Size | SHA256 Time | Throughput | Signature |
|-----------|-------------|------------|-----------|
| 10 MB | ~50 ms | ~200 MB/s | Parallel |
| 1 GB | ~5 s | ~200 MB/s | Parallel |
| 4 GB | ~20 s | ~200 MB/s | Parallel |

*Throughput depends on disk speed. SSD recommended. Signature check runs concurrently — does not add to wall-clock time for PE files.*

Non-PE files skip signature checking entirely (`Signature: skipped (not a PE file)`).
```

**Section 8: Why PowerShell 7?**
```markdown
## ❓ Why PowerShell 7?

PowerShell 7 (pwsh) is a modern, cross-platform shell built on .NET. It is **not** the same as Windows PowerShell 5.1 that comes with Windows.

- ✅ Cross-platform (Windows, Linux, macOS)
- ✅ `Start-ThreadJob` for parallel execution
- ✅ Modern .NET hashing APIs
- ✅ Runs alongside Windows PowerShell — no conflicts

Install: `winget install Microsoft.PowerShell` (Windows) or [see instructions](https://learn.microsoft.com/en-us/powershell/scripting/install/installing-powershell).
```

**Section 9: PowerShell Profile Integration**
Check if `Microsoft.PowerShell_profile_example.ps1` exists in the repo. If it does, use its content as the example. If not, provide a generic alias example:
```markdown
## ⚙️ PowerShell Profile Integration

Add to your `$PROFILE` for quick access:

```powershell
function verihash { & "C:\path\to\VeriHash.ps1" @args }
```

Then use from anywhere:
```powershell
verihash file.exe
verihash *.dll -Manifest
```

See `Microsoft.PowerShell_profile_example.ps1` in the repo for a full example.
```

**Section 10: Privacy**
```markdown
## 🔒 Privacy

- **No network calls** — hashing is entirely local
- **No telemetry** — nothing is sent anywhere
- **No VirusTotal** — removed in v2 (was never shipped in v1 releases)
- **Optional logging** — only with explicit `-Log` flag or `$env:VERIHASH_LOG=1`
- **Log privacy** — file paths logged without user-home prefix; hash values never logged in full
```

**Section 11: Contributing**
```markdown
## 🤝 Contributing

1. Fork and clone
2. Create a branch
3. Make changes
4. Run tests: `.\Test-All.ps1`
5. Submit a pull request

Please follow existing code conventions: `[CmdletBinding()]`, `[OutputType()]`, 4-space indentation, `Verb-Noun` function names.
```

**Section 12: Running Tests**
```markdown
## 🧪 Running Tests

```powershell
# Full suite (tests + linter + profiler)
.\Test-All.ps1

# Tests only
.\Test-All.ps1 -SkipAnalyzer -SkipProfiler

# Single test file
Invoke-Pester -Path Tests/VeriHash.Core.Module.Tests.ps1 -Output Detailed
Invoke-Pester -Path Tests/VeriHash.Cli.Tests.ps1 -Output Detailed

# CI mode (exit code on failure)
.\Test-All.ps1 -CI
```

Required: `Install-Module Pester -Scope CurrentUser` and `Install-Module PSScriptAnalyzer -Scope CurrentUser`.
```

**Section 13: License**
```markdown
## 📄 License

[AGPL-3.0-or-later](LICENSE.md) — © 2024-2025 arcticpinecone
```

**Section 14: Breaking Changes callout (per D-09 "breaking changes called out")**
Add a brief section after Features or at the bottom:
```markdown
## ⚠️ Upgrading from v1

v2.0 is a clean break. Key changes:

| v1 | v2 |
|----|-----|
| `.\VeriHash.ps1 file.exe -Hash "abc..."` | Copy hash to clipboard, then `.\VeriHash.ps1 file.exe` |
| `-Algorithm MD5,SHA512` | SHA256 always; MD5/SHA512 via module API |
| `-OnlyVerify` | Removed — verification is automatic |
| `-SkipSignatureCheck` | Removed — non-PE files auto-skip |
| `-Force` | Removed — sidecar conflicts resolved interactively |
| `-SendTo` | `-InstallSendTo` |
| PSFramework optional | Not used — plain-text logging built in |
| `QuickHash.ps1` | Removed — v2 hot-path replaces its purpose |

See [CHANGELOG.md](CHANGELOG.md) for the complete list.
```

**Do NOT include:**
- QuickHash section (file deleted per CLEAN-01)
- VirusTotal section (removed in Phase 4)
- PSFramework references (removed in Phase 4)
- File picker dialog (replaced by help banner per D-03)
- Any v1-specific parameter documentation
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "
        $r = Get-Content 'README.md' -Raw;
        if ($r -match 'version-2\.0\.0') { Write-Host 'PASS: v2.0.0 badge' } else { Write-Host 'FAIL: missing v2.0.0 badge' };
        if ($r -match '## .* Module Architecture') { Write-Host 'PASS: Module Architecture section' } else { Write-Host 'FAIL: missing Module Architecture' };
        if ($r -match 'VeriHash\.Core') { Write-Host 'PASS: Core module documented' } else { Write-Host 'FAIL: Core module missing' };
        if ($r -match 'VeriHash\.HotPath') { Write-Host 'PASS: HotPath module documented' } else { Write-Host 'FAIL: HotPath module missing' };
        if ($r -match 'VeriHash\.Manifest') { Write-Host 'PASS: Manifest module documented' } else { Write-Host 'FAIL: Manifest module missing' };
        if ($r -match '-InstallSendTo') { Write-Host 'PASS: -InstallSendTo documented' } else { Write-Host 'FAIL: -InstallSendTo missing' };
        if ($r -match '-Manifest') { Write-Host 'PASS: -Manifest documented' } else { Write-Host 'FAIL: -Manifest missing' };
        if ($r -match 'Breaking') { Write-Host 'PASS: Breaking changes section' } else { Write-Host 'FAIL: no breaking changes callout' };
        if ($r -notmatch 'QuickHash') { Write-Host 'PASS: No QuickHash section' } else { Write-Host 'FAIL: QuickHash still referenced' };
        if ($r -notmatch 'PSFramework') { Write-Host 'PASS: No PSFramework reference' } else { Write-Host 'FAIL: PSFramework still referenced' };
        if ($r -notmatch 'VirusTotal') { Write-Host 'PASS: No VirusTotal reference' } else { Write-Host 'FAIL: VirusTotal still referenced' };
        if ($r -match 'CHANGELOG\.md') { Write-Host 'PASS: Links to CHANGELOG' } else { Write-Host 'FAIL: no CHANGELOG link' };
        if ($r -match 'No external dependencies') { Write-Host 'PASS: No-deps install documented' } else { Write-Host 'FAIL: install path unclear' };
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - README.md contains version badge with `2.0.0` (per D-12)
    - README.md contains `> **v2.0 — Modular Rewrite**` tagline linking to CHANGELOG (per D-12)
    - README.md contains `## ✨ Features` with manifest mode, multi-file batch, prefixed clipboard
    - README.md contains `## 📋 Requirements` with "No external dependencies. PSFramework is **not** required"
    - README.md contains `## 💻 Usage` with hash, multi-file, manifest create, manifest verify examples
    - README.md contains all v2 switches in a table: -Manifest, -InstallSendTo, -InstallKDE, -NoPause, -SystemWide, -Log, -Help
    - README.md contains `## 🖥️ OS Integration` with SendTo and KDE sections
    - README.md contains `## 🏗️ Module Architecture` documenting VeriHash.Core, VeriHash.HotPath, VeriHash.Manifest, VeriHash.Integrations.ps1
    - README.md contains `## ⚡ Speed Benchmarks`
    - README.md contains `## ⚠️ Upgrading from v1` with dropped params table
    - README.md does NOT contain `QuickHash` (CLEAN-01 deleted it)
    - README.md does NOT contain `PSFramework` (removed in Phase 4)
    - README.md does NOT contain `VirusTotal` (removed in Phase 4)
    - README.md does NOT contain `-Hash` or `-Algorithm` or `-OnlyVerify` as current v2 parameters
    - README.md does NOT contain `Interactive GUI` or `file picker` (replaced by help banner per D-03)
  </acceptance_criteria>
  <done>README.md is a comprehensive v2 document with module architecture, v2 CLI surface, breaking changes callout, and no references to deleted/removed v1 features.</done>
</task>

<task type="auto">
  <name>Task 2: Update CHANGELOG.md with v2.0.0 entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — lines 1-8 for header pattern, lines 11-30 for [Unreleased] section, lines 32-65 for v1.3.0 entry format, lines 142+ for older entries)
    - .planning/phases/05-thin-cli-cleanup-docs/05-CONTEXT.md (D-10 CHANGELOG structure)
    - .planning/phases/05-thin-cli-cleanup-docs/05-PATTERNS.md (CHANGELOG header and entry format patterns)
  </read_first>
  <action>
Replace the entire contents of CHANGELOG.md with the v2.0 entry structure per D-10. The file structure MUST be:

**Header:**
```markdown
# VeriHash Changelog

## Version History

- Current stable release: [v2.0.0]
- Version 1.x history is collapsed below.

---
```

**v2.0.0 entry:**
```markdown
### [v2.0.0] (Current)

Version: 2.0.0
> Modular Rewrite — focused modules replace the monolith

**ARCHITECTURE:**

- 🏗️ **Modular rewrite**: The ~900-line `VeriHash.ps1` monolith is replaced by a ≤200-line thin CLI dispatcher and three focused PowerShell modules:
  - **VeriHash.Core** — hashing, clipboard parsing, sidecar verify, formatting, logging, platform detection
  - **VeriHash.HotPath** — PE-only Authenticode, parallel hash + signature via ThreadJob, multi-file batch loop with tally
  - **VeriHash.Manifest** — GNU `sha256sum`-compatible manifest create and verify with atomic writes, path-traversal guard, machine-readable exit codes

**NEW FEATURES:**

- 📦 **Manifest mode**: Create and verify `sha256sum`-compatible manifests
  - `.\VeriHash.ps1 file1.txt, file2.txt -Manifest` — creates manifest
  - `.\VeriHash.ps1 manifest.sha256 -Manifest` — verifies manifest (extension auto-detect)
  - Exit codes: 0 (all pass), 1 (mismatch), 2 (missing), 3 (parse error)
  - Atomic writes via temp-file-then-rename (no partial manifests on Ctrl+C)
  - Path traversal guard rejects entries escaping manifest directory
- 🔄 **Multi-file batch mode**: Process multiple files in a single invocation
  - Full result per file (hash, sidecar, clipboard, signature)
  - Final tally: `X/N matched, Y mismatch, Z missing`
- 📋 **Prefixed clipboard parsing**: `sha256:71792c...` prefix form is recognized alongside plain hex
  - Explicit prefix overrides length-based algorithm inference
- ⚡ **PE-only signature checking**: Non-PE files skip Authenticode (no `MZ` header → instant skip)
- 🔀 **Parallel hash + signature**: ThreadJob concurrency for PE files (wall-clock ≈ max, not sum)
- 📝 **Built-in plain-text logging**: `-Log` flag or `$env:VERIHASH_LOG=1` writes to `~/.verihash/verihash.log`
- 🖥️ **Manifest SendTo shortcut**: `-InstallSendTo` now creates both `VeriHash.lnk` and `VeriHash - Manifest.lnk`
- 🐧 **KDE manifest action**: KDE context menu includes Compute Hash, Verify Hash, and Manifest Hash actions

**⚠️ BREAKING CHANGES:**

- **Dropped parameters:**
  - `-Hash` / `-InputHash` — copy hash to clipboard instead
  - `-Algorithm` — SHA256 is always computed; use module API for others
  - `-OnlyVerify` — verification is automatic when sidecar/clipboard match exists
  - `-SkipSignatureCheck` — non-PE files are auto-skipped; PE files always check
  - `-Force` — sidecar conflicts are handled interactively
  - `-LogLevel` — replaced by simple `-Log` on/off switch
- **Renamed parameters:**
  - `-SendTo` → `-InstallSendTo` (clearer intent)
- **Removed features:**
  - ❌ **VirusTotal integration** — removed entirely (scope creep; never shipped in releases)
  - ❌ **PSFramework dependency** — replaced by built-in plain-text logger
  - ❌ **QuickHash.ps1** — removed (v2 hot-path replaces its purpose)
  - ❌ **VeriHash.LogUtils.ps1** — removed (log analysis utilities, no v2 consumer)
  - ❌ **VeriHash.Config.ps1** — removed (v2 has no config file system)
  - ❌ **VeriHash-OpenWith.bat** — removed (replaced by proper .lnk shortcuts)
  - ❌ **Interactive file picker** — replaced by help banner with usage examples
- **Behavior changes:**
  - `[string[]]$FilePath` replaces `[string]$FilePath` (array input for multi-file)
  - Pause-at-end auto-detects GUI launch; terminal sessions skip pause automatically
  - No-args invocation shows help banner instead of file picker dialog

---
```

**Collapsed v1 history:**
Take the entire content from the current CHANGELOG.md starting from `### [Unreleased]` through the end of the file. Wrap it in a collapsible section:

```markdown
<details>
<summary>📜 Version 1.x History</summary>

### [Unreleased]
[... existing unreleased content ...]

### [v1.3.0]
[... existing v1.3.0 content ...]

### [v1.2.6]
[... existing v1.2.6 content ...]

[... all remaining v1 entries ...]

</details>
```

Preserve ALL existing v1 entries exactly as-is inside the collapsed section. Do not edit, summarize, or truncate them.
  </action>
  <verify>
    <automated>
      powershell -NoProfile -Command "
        $c = Get-Content 'CHANGELOG.md' -Raw;
        if ($c -match 'v2\.0\.0.*Current') { Write-Host 'PASS: v2.0.0 entry exists' } else { Write-Host 'FAIL: no v2.0.0 entry' };
        if ($c -match 'Modular Rewrite') { Write-Host 'PASS: Modular Rewrite framing' } else { Write-Host 'FAIL: not framed as Modular Rewrite' };
        if ($c -match 'BREAKING CHANGES') { Write-Host 'PASS: Breaking changes section' } else { Write-Host 'FAIL: no breaking changes' };
        if ($c -match 'Manifest mode') { Write-Host 'PASS: Manifest mode listed' } else { Write-Host 'FAIL: manifest mode missing' };
        if ($c -match 'Multi-file batch') { Write-Host 'PASS: Multi-file batch listed' } else { Write-Host 'FAIL: multi-file missing' };
        if ($c -match 'VirusTotal.*removed') { Write-Host 'PASS: VT removal noted' } else { Write-Host 'FAIL: VT removal not noted' };
        if ($c -match 'PSFramework.*replaced') { Write-Host 'PASS: PSFramework removal noted' } else { Write-Host 'FAIL: PSFramework removal not noted' };
        if ($c -match 'QuickHash.*removed') { Write-Host 'PASS: QuickHash removal noted' } else { Write-Host 'FAIL: QuickHash removal not noted' };
        if ($c -match '<details>') { Write-Host 'PASS: v1 history collapsed' } else { Write-Host 'FAIL: v1 history not collapsed' };
        if ($c -match 'v1\.3\.0') { Write-Host 'PASS: v1.3.0 preserved' } else { Write-Host 'FAIL: v1.3.0 missing' };
        if ($c -match 'v1\.2\.6') { Write-Host 'PASS: v1.2.6 preserved' } else { Write-Host 'FAIL: v1.2.6 missing' };
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md header says `Current stable release: [v2.0.0]`
    - CHANGELOG.md contains `### [v2.0.0] (Current)` entry
    - Entry includes `> Modular Rewrite` tagline
    - Entry has **ARCHITECTURE:** section listing Core, HotPath, Manifest modules
    - Entry has **NEW FEATURES:** section listing manifest mode, multi-file batch, prefixed clipboard, PE-only sig, parallel hash+sig, built-in logging, manifest SendTo shortcut
    - Entry has **⚠️ BREAKING CHANGES:** section listing dropped params (-Hash, -Algorithm, -OnlyVerify, -SkipSignatureCheck, -Force, -LogLevel), renamed param (-SendTo → -InstallSendTo), removed features (VirusTotal, PSFramework, QuickHash, LogUtils, Config, OpenWith.bat, file picker)
    - All existing v1 entries (Unreleased, v1.3.0, v1.2.6, v1.2.5, etc.) are wrapped in `<details><summary>📜 Version 1.x History</summary>...</details>`
    - v1 entry content is preserved exactly, not summarized or truncated
  </acceptance_criteria>
  <done>CHANGELOG.md has a clean v2.0.0 entry with architecture, new features, and breaking changes sections. All v1 history is preserved in a collapsed details/summary block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README examples → user clipboard | Users may copy example commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | README.md examples | mitigate | Use generic filenames in examples (`file.exe`, `file.txt`); no real paths, credentials, API keys, or hash values that could be confused with real data |
| T-05-07 | Spoofing | Badge URLs | accept | Badges link to official GitHub/shields.io; standard open-source practice; no custom badge servers |
</threat_model>

<verification>
1. README.md contains v2.0.0 badge, Module Architecture section, v2 CLI surface, breaking changes callout
2. README.md does NOT contain QuickHash, PSFramework, VirusTotal, -Hash param, file picker
3. CHANGELOG.md has v2.0.0 entry with architecture, new features, breaking changes
4. CHANGELOG.md v1 history is preserved in collapsed `<details>` block
5. No credentials, real paths, or sensitive data in examples
</verification>

<success_criteria>
- README.md is a comprehensive v2 document matching D-09 section structure
- CHANGELOG.md has a v2.0.0 entry matching D-10 structure with breaking changes
- Both files link to each other (README → CHANGELOG, CHANGELOG standalone)
- No v1 artifacts referenced as current (QuickHash, PSFramework, VirusTotal, old params)
- v1 history preserved, not deleted
</success_criteria>

<output>
After completion, create `.planning/phases/05-thin-cli-cleanup-docs/05-03-SUMMARY.md`
</output>
