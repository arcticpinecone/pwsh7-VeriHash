---
phase: 01-privacy-logging-compliance
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - Verihash Logging Concepting.md
autonomous: true
requirements: [PRIV-04]
must_haves:
  truths:
    - "Logging guide example shows realistic 16-char truncated hash, not 6-char placeholder"
    - "Logging guide example includes Hash field in Data block"
    - "Logging guide example uses correct FunctionName (Get-And-SaveHash)"
    - "Logging guide mentions config path sanitization in the Privacy-First Design section"
    - "Guide describes actual code behavior with zero implementation caveats"
  artifacts:
    - path: "Verihash Logging Concepting.md"
      provides: "Accurate logging guide matching code behavior"
      contains: "D7A8FBB307D78094..."
  key_links:
    - from: "Verihash Logging Concepting.md example"
      to: "VeriHash.ps1 Get-And-SaveHash log output"
      via: "example matches actual code output format"
      pattern: "D7A8FBB307D78094"
---

<objective>
Update the Verihash Logging Concepting.md guide so it accurately describes the code's actual behavior after privacy fixes — closing PRIV-04.

Purpose: The logging guide is the user-facing contract. After PRIV-01/PRIV-02/PRIV-03/LOGC-02 code changes, the guide must reflect reality: 16-char hash truncation, config path sanitization, correct function names.

Output: Updated Verihash Logging Concepting.md with zero discrepancies between documented and actual behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-privacy-logging-compliance/01-RESEARCH.md
@.planning/phases/01-privacy-logging-compliance/01-PATTERNS.md
@.planning/phases/01-privacy-logging-compliance/01-01-SUMMARY.md

<interfaces>
<!-- Current content of the logging guide sections that need updating -->

From "Verihash Logging Concepting.md" lines 96-115 — Example Log Entry:
```json
{
  "Timestamp": "2025-01-17T10:30:45.123Z",
  "Level": "Verbose",
  "Message": "Hash computed: ABC123...",
  "FunctionName": "Invoke-ComputeHash",
  "ModuleName": "VeriHash",
  "Tags": ["Hash", "Result"],
  "ComputerName": "DESKTOP-ABC123",
  "Username": "username",
  "Data": {
    "Path": "%USERPROFILE%\\Downloads\\file.exe",
    "Algorithm": "SHA256",
    "DurationMs": 42.5,
    "ThroughputMBs": 190.2
  }
}
```

From "Verihash Logging Concepting.md" lines 1-11 — Privacy-First Design:
```markdown
## Privacy-First Design

VeriHash logging follows **data minimization principles** (GDPR Article 5(1)(c), CWE-532, OWASP guidelines):

- **Paths are sanitized**: `C:\Users\username\...` becomes `%USERPROFILE%\...` (Windows) or `~` (Linux/macOS)
- **Opt-in only**: Logging is disabled by default
- **Note**: PSFramework adds ComputerName/Username metadata that cannot be disabled at the provider level

Path sanitization is the primary privacy protection—file paths often contain usernames and reveal directory structures.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update logging guide to match actual code behavior (PRIV-04)</name>
  <files>Verihash Logging Concepting.md</files>
  <read_first>
    - Verihash Logging Concepting.md (full file — need to see all sections for four targeted fixes)
    - .planning/phases/01-privacy-logging-compliance/01-PATTERNS.md (Documentation update section with all 4 fixes)
  </read_first>
  <action>
Apply four targeted fixes to `Verihash Logging Concepting.md`:

**Fix 1: Update example log entry hash in Message field (line 102)**

Replace:
```json
  "Message": "Hash computed: ABC123...",
```

With:
```json
  "Message": "Hash computed: D7A8FBB307D78094...",
```

Rationale: `ABC123...` implies 6-char truncation. The actual code truncates to 16 chars. `D7A8FBB307D78094` is the first 16 chars of the SHA256 of "The quick brown fox jumps over the lazy dog" — a realistic example that exactly matches the 16-char truncation pattern implemented in Get-And-SaveHash.

**Fix 2: Update FunctionName in example (line 103)**

Replace:
```json
  "FunctionName": "Invoke-ComputeHash",
```

With:
```json
  "FunctionName": "Get-And-SaveHash",
```

Rationale: The actual function that produces this log entry is `Get-And-SaveHash` (VeriHash.ps1 line 839 context). `Invoke-ComputeHash` does not exist in the codebase.

**Fix 3: Add Hash field to Data block in example (lines 108-113)**

Replace the Data block:
```json
  "Data": {
    "Path": "%USERPROFILE%\\Downloads\\file.exe",
    "Algorithm": "SHA256",
    "DurationMs": 42.5,
    "ThroughputMBs": 190.2
  }
```

With:
```json
  "Data": {
    "Path": "%USERPROFILE%\\Downloads\\file.exe",
    "Algorithm": "SHA256",
    "Hash": "D7A8FBB307D78094...",
    "DurationMs": 42.5,
    "ThroughputMBs": 190.2
  }
```

Rationale: The actual code includes `Hash = $truncatedHash` in the -Data block (after PRIV-01 fix). The guide must show the same fields.

**Fix 4: Add config path sanitization to Privacy-First Design section (around line 10)**

After the existing line:
```
Path sanitization is the primary privacy protection—file paths often contain usernames and reveal directory structures.
```

Add:
```markdown

- **Hash values are truncated**: Only the first 16 characters of hash values appear in logs (e.g., `D7A8FBB307D78094...`), preventing file fingerprinting via hash correlation
- **Config paths are sanitized**: Configuration file paths (`ConfigDirectory`, `ConfigFile`) are sanitized using the same `ConvertTo-SanitizedPath` function as hash operation paths
```

This adds two new bullet points that reflect the PRIV-01 hash truncation and PRIV-02 config path sanitization behaviors that now exist in code.
  </action>
  <verify>
    <automated>pwsh -NoProfile -Command "Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'D7A8FBB307D78094' | Measure-Object | Select-Object -ExpandProperty Count"</automated>
  </verify>
  <acceptance_criteria>
    - "Verihash Logging Concepting.md" contains `"Message": "Hash computed: D7A8FBB307D78094..."` (not `ABC123...`)
    - "Verihash Logging Concepting.md" contains `"FunctionName": "Get-And-SaveHash"` (not `Invoke-ComputeHash`)
    - "Verihash Logging Concepting.md" contains `"Hash": "D7A8FBB307D78094..."` inside the Data block of the example
    - "Verihash Logging Concepting.md" contains `Hash values are truncated` in the Privacy-First Design section
    - "Verihash Logging Concepting.md" contains `Config paths are sanitized` in the Privacy-First Design section
    - `Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'ABC123'` returns NO matches (old placeholder removed)
    - `Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'Invoke-ComputeHash'` returns NO matches (wrong function name removed)
    - `Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'D7A8FBB307D78094'` returns at least 3 matches (Message, Data.Hash, truncation bullet)
  </acceptance_criteria>
  <done>Logging guide accurately describes 16-char hash truncation, correct function name, Hash field in Data, and config path sanitization. Zero discrepancies between guide and code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → User expectations | Guide sets user expectations about what data appears in logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Information Disclosure | Logging guide accuracy | mitigate | Update guide to match actual truncation (16 chars) and sanitization behavior. Inaccurate guide could lead users to believe logs contain less data than they do, or vice versa (PRIV-04). |
</threat_model>

<verification>
1. `pwsh -NoProfile -Command "Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'D7A8FBB307D78094'"` — returns 3+ matches
2. `pwsh -NoProfile -Command "Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'ABC123'"` — returns 0 matches
3. `pwsh -NoProfile -Command "Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'Invoke-ComputeHash'"` — returns 0 matches
4. `pwsh -NoProfile -Command "Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'Get-And-SaveHash'"` — returns 1+ matches
5. `pwsh -NoProfile -Command "Select-String -Path 'Verihash Logging Concepting.md' -Pattern 'Config paths are sanitized'"` — returns 1 match
</verification>

<success_criteria>
- Logging guide example shows realistic 16-char truncated hash value
- Guide example includes Hash field in Data block
- Guide example uses correct FunctionName (Get-And-SaveHash)
- Privacy-First Design section mentions hash truncation and config path sanitization
- Zero aspirational claims — guide describes actual implemented behavior only
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-logging-compliance/01-03-SUMMARY.md`
</output>
