---
phase: 02-ci-cd-pipeline
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: []
autonomous: false
requirements: [CICD-01, CICD-02]

must_haves:
  truths:
    - "Pushing a commit to dev triggers a GitHub Actions workflow run"
    - "The test job runs on both ubuntu-latest and windows-latest and passes"
    - "The lint job runs on ubuntu-latest and passes"
    - "All 3 jobs show green checkmarks in the Actions tab"
  artifacts: []
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "GitHub Actions runner"
      via: "push event triggers workflow"
      pattern: "Actions tab shows 'CI' workflow run"
---

<objective>
Verify the CI pipeline triggers and passes on GitHub Actions after pushing the workflow.

Purpose: Confirm that the locally-created ci.yml actually triggers on GitHub, that both OS runners succeed, and that the lint job passes — proving CICD-01 and CICD-02 are operational end-to-end.
Output: Verified green CI run on GitHub Actions
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-ci-cd-pipeline/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify CI pipeline on GitHub Actions</name>
  <what-built>
    Plan 02-01 created `.github/workflows/ci.yml` with:
    - A `test` job running Pester on ubuntu-latest + windows-latest (parallel matrix)
    - A `lint` job running PSScriptAnalyzer against all 3 `.ps1` files on ubuntu-latest
    - Path filters on `**.ps1`, `Tests/**`, `PSScriptAnalyzerSettings.psd1`
    - Pester pinned to 5.x (`MaximumVersion 5.99`)

    Plan 02-01 also updated `Test-All.ps1` to lint all 3 files locally (this `.ps1` change ensures the path filter matches on first push).
  </what-built>
  <how-to-verify>
    1. Push the `dev` branch to GitHub: `git push origin dev`
    2. Open the repository on GitHub: https://github.com/arcticpinecone/pwsh7-VeriHash
    3. Navigate to the **Actions** tab
    4. Verify a workflow run named **"CI"** was triggered by the push
    5. Click into the run and verify **3 jobs** are listed:
       - `Tests (ubuntu-latest)` — should complete with green ✅
       - `Tests (windows-latest)` — should complete with green ✅
       - `PSScriptAnalyzer` — should complete with green ✅
    6. Click into the `Tests (ubuntu-latest)` job → expand "Run Pester Tests" step → verify test output shows all tests passing
    7. Click into the `PSScriptAnalyzer` job → expand "Run PSScriptAnalyzer" step → verify output shows `✅` for all 3 files

    **If path filter prevents trigger** (Pitfall 4 — first push may not match if only workflow file changed):
    - The `Test-All.ps1` modification is a `.ps1` file, so it should trigger the path filter.
    - If no run appears, use the "Run workflow" button (workflow_dispatch) on the Actions tab to trigger manually.

    **Expected result:** All 3 jobs green. No failures. Pester tests pass on both OSes. PSScriptAnalyzer shows clean for all 3 files.
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 jobs passed green, or describe any failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced by this verification plan.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No threats — this plan is verification-only, no code changes |
</threat_model>

<verification>
All 3 GitHub Actions jobs (Tests ubuntu, Tests windows, PSScriptAnalyzer) show green checkmarks.
</verification>

<success_criteria>
- GitHub Actions "CI" workflow triggers on push to dev
- All 3 jobs pass (2 test matrix jobs + 1 lint job)
- No manual fixes needed after push
</success_criteria>

<output>
After completion, create `.planning/phases/02-ci-cd-pipeline/02-02-SUMMARY.md`
</output>
